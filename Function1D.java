public interface Function1D {
    double apply(double x);//we can use lambdas to declare functions because the interface has only one method
    //lambda syntax is: interface name = parameter -> returned expression  "->" means returns
}
